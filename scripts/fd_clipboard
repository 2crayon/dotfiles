#!/usr/bin/env bash
searchpath=$1

chosen=$(fd . $searchpath --hidden | fzf --scheme=path)
# fzf 130 exit code: Interrupted with CTRL-C or ESC
[[ $? == 130 ]] && exit 1

echo "$chosen" | nohup >/dev/null 2>/dev/null xclip -sel clip -rmlastnl